syntax = "proto3";

package PingPongPackage;

service PingPong {
  rpc Ping(ClientIdentity) returns (PongMessage);
}

message ClientIdentity {
  string id = 1;
}

message PongMessage {
  string message = 1;
}